# Core computer vision
opencv-contrib-python>=4.8.0
numpy>=1.24.0

# GUI
Pillow>=10.0.0
tkcalendar>=1.6.1

# Face recognition (primary engine — requires cmake + dlib)
# Install guide: https://github.com/ageitgey/face_recognition#installation
# setuptools<81 pinned: face_recognition_models still imports pkg_resources,
# which setuptools removed in 81 (needed on Python 3.13+/3.14 where it no
# longer ships with the interpreter by default).
setuptools<81
dlib-bin; sys_platform == 'linux'
face_recognition>=1.3.0
face_recognition_models @ git+https://github.com/ageitgey/face_recognition_models

# Data / export
scikit-learn>=1.3.0
matplotlib>=3.7.0
pandas>=2.0.0
openpyxl>=3.1.0

# Web UI Migration
pywebview>=6.2.1
fastapi>=0.110.0
uvicorn>=0.30.0
python-multipart>=0.0.9
